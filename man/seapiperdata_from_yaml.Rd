% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_seapiperdata.R
\name{seapiperdata_from_yaml}
\alias{seapiperdata_from_yaml}
\title{Build seaPiperData from a YAML file of RDS paths}
\usage{
seapiperdata_from_yaml(yaml_file, primary_id = "PrimaryID")
}
\arguments{
\item{yaml_file}{path to a YAML file describing RDS locations}

\item{primary_id}{name of the primary gene ID column}
}
\description{
Load a \code{seaPiperData} object from a YAML file that points to serialized R
objects (\code{.rds}). Relative paths are resolved relative to the YAML file
itself.
}
\details{
The YAML must define one or more datasets in a top-level \code{datasets}
mapping.

Supported dataset keys and expected \code{.rds} object types are:
\itemize{
\item \code{annot}: \code{data.frame} with gene annotation (optionally includes \code{primary_id})
\item \code{cntr}: named \code{list} of contrast \code{data.frame}s
\item \code{tmod_res}: named \code{list} of tmod results (per contrast)
\item \code{tmod_dbs}: named \code{list} of tmod DB objects (or wrappers with \code{dbobj})
\item \code{tmod_map}: tmod mapping object (\code{get_tmod_mapping} output)
\item \code{tmod_gl}: tmod gene-ranking object (\code{gl.rds}-like nested structure)
\item \code{config}: \code{list} with sea-snap style config fields
\item \code{covar}: \code{data.frame} of sample covariates (required)
\item \code{rld}: numeric matrix/data.frame (genes x samples)
\item \code{pca}: PCA scores matrix/data.frame (samples x PCs)
}
}
\examples{
# # Example YAML structure (all paths are relative to this YAML file):
# datasets:
#   default:
#     annot: annot.rds
#     cntr: cntr.rds
#     tmod_res: tmod_res.rds
#     tmod_dbs: tmod_dbs.rds
#     tmod_map: tmod_map.rds
#     tmod_gl: tmod_gl.rds
#     config: config.rds
#     covar: covar.rds
#     rld: rld.rds
}
