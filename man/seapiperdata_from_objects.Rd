% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/make_seapiperdata.R
\name{seapiperdata_from_objects}
\alias{seapiperdata_from_objects}
\title{Build seaPiperData from basic inputs}
\usage{
seapiperdata_from_objects(
  cntr,
  annot,
  exprs,
  primary_id = "PrimaryID",
  covar = NULL,
  config = NULL,
  title = NULL
)
}
\arguments{
\item{cntr}{named list of contrast data frames}

\item{annot}{annotation data frame}

\item{exprs}{expression matrix (genes x samples)}

\item{primary_id}{primary gene identifier column name}

\item{covar}{optional covariate data frame (samples x variables)}

\item{config}{optional configuration list; if NULL, defaults are created}

\item{title}{optional dataset title stored in \code{config$dataset_title}}
}
\description{
Create a \code{seaPiperData} object from contrasts, annotations, and expression
data without requiring a pipeline object.
}
\details{
If you pass a custom \code{config}, it must include:
\code{organism} (list with \code{name}, \code{taxon}),
\code{experiment} (list with \code{design_formula}),
\code{contrasts} (list with \code{contrast_list}, each having \code{ID} and \code{title}),
\code{tmod} (list with \code{databases} and \code{sort_by}),
\code{filter} (list with \code{low_counts}, \code{min_counts}, \code{min_count_n}),
\code{dataset_title} (string, defaults to "default").
}
